import AbiJaTugiPage from '@/views/settings/AbiJaTugiPage'

// Standalone route page (/app/help) — renders the shared Help & Support view
// without a back button (navigation is handled by the sidebar).
export default function HelpPage() {
  return <AbiJaTugiPage />
}
